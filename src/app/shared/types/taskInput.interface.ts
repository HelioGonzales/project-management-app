export interface TaskInputInterface {
  // title: string;
  // order: number;
  // description: string;
  // userId: string;
  // users: string[];
  title: string;
  boardId: string;
  columnId: string;
}
